# Frontend Hospitals Administration

This project was make in Angular version 9.0.2.

## Setup

Run `npm install`.

Run `npm start`. Navigate to `http://localhost:4200/`.

##Note

The Backend must be running at `http://localhost:8080/`.
